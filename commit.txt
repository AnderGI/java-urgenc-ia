feat(backoffice/product/register-product): controller and acceptance test added

ProductPutControllerShould class asserts that the put request is going against the products resource. It only tests the request path, request json body (and its parsing) and the 202 Accepted status code.
